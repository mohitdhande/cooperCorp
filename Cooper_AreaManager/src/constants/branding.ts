// Splash video size (app/index.tsx). Kept separate from LOGIN_LOGO_SIZE
// since the two are allowed to differ (login's logo is smaller than the
// splash video).
export const SPLASH_VIDEO_SIZE = 160;

// Login screen logo size (screens/login.tsx `brandLogo` style) — change
// this one value to resize the logo there.
export const LOGIN_LOGO_SIZE = 110;

// The small circular video shown by LoadingOverlay for screen-level
// blocking loads (shared/LoadingOverlay.tsx).
export const LOADING_OVERLAY_VIDEO_SIZE = 82;
